# Contributing

Install [Bun](https://bun.sh), then run:

```bash
bun install
bun run check
bun run test
bun run build
```

Keep changes small, add tests for behavior changes, and avoid adding runtime
dependencies unless they make the package simpler.
